// Shared across every EnhancedTable-based game table (Owned/Wishlist in
// GameLibrarySection, Progress/Play Sessions in GameProgressSection) so the Platform
// column — the one column every one of these tables has in common — renders at an
// identical width everywhere, not just within one section's own sibling tables.
export const PLATFORM_COLUMN_WIDTH = 170;
