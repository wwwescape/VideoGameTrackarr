# TODOs

- [x] **Tags: permanent deletion UI + consolidated input, plus Settings sub-nav restructuring.** GH #10 asked for a way to delete tags. Turns out `DELETE /api/tags/{tag_id}` (`backend/app/api/routes/tags.py`) and the frontend `useDeleteTag` hook (`frontend/hooks/useTags.ts`) already existed and correctly cascade-clean `GameTag`/`DeviceTag`/`AccessoryTag` join rows — but nothing in the UI called `useDeleteTag`; there was no tag-management surface at all, only per-item detach (the "x" on a chip in `TagsSection.tsx` removes a tag from *that* game/device/accessory, not the tag itself). One tag vocabulary, two entry points with deliberately different removal semantics:
  - **Game/Device/Accessory Details page** — add existing or create-new tags, but the delete/remove action here only **detaches** the tag from that one item (`DELETE /api/games/{id}/tags/{tag_id}` etc.) — other items keep it, since other games/devices/accessories may share the same tag.
  - **Settings → Tag Manager** — the only place a tag can be **permanently deleted** (`DELETE /api/tags/{tag_id}`), removing it from every game/device/accessory that has it.
  - **Phase 1 — consolidated tag input (Details pages) — done.** Replaced the split UI in `TagsSection.tsx` (a `Chip` row plus a separate freeSolo `Autocomplete` below it) with a single MUI `Autocomplete` in `multiple` + `freeSolo` mode — chips render inline in the field itself, dispatching on the `onChange` reason (`selectOption`/`createOption`/`removeOption`) rather than diffing the value array. No new dependency needed (`multiple` mode already ships in the installed MUI version). Along the way, found and fixed a separate pre-existing bug in `frontend/hooks/useTags.ts`: the attach/detach hooks invalidated a numeric-id-keyed query (`["games", gameId]`) while `useGame`/`useDeviceItem`/`useAccessoryItem` cache by the route's string identifier (slug/uuid) — so attach/detach/create never visually updated the current detail page without a reload. Broadened those 6 hooks to invalidate the bare `["games"|"devices"|"accessories"]` prefix, matching `useDeleteTag`'s existing pattern in the same file.
  - **Phase 2 — Settings sub-nav + Tag Manager page — done.** New `SettingsSubNav.tsx` (mirrors `InsightsSubNav.tsx`) rendered atop three pages: `Settings.tsx` (now "Overview" — Jobs section moved out), a new `TagManagerPage.tsx` at `/settings/tags` (lists all tags as delete-able `Chip`s, `ConfirmDialog` warns it removes the tag from every game/device/accessory), and a new `JobsPage.tsx` at `/settings/jobs` (just relocates the existing self-contained `JobsSection`). `destinations.tsx` gained `subItems` on the Settings entry for the desktop drawer. New breadcrumb entries and i18n strings (`nav.tagManager`/`nav.jobs`, `settings.subNav.overview`, `settings.tagManager.*`) across all 4 locales.
  - **Follow-up: Tag Manager full CRUD + bulk delete — done**, per on-screen feedback that the phase-2 page ("just a `Chip` list") was too basic. Rewrote `TagManagerPage.tsx` around the app's existing `EnhancedTable.tsx` (the same sortable/paginated/multiselect table already used by `GameLibrarySection.tsx`) instead of inventing a new list pattern — gives List/Add/Edit/Delete plus multiselect bulk-delete for free. New backend `PATCH /api/tags/{tag_id}` (`tag_service.update_tag` rejects a rename that collides with a *different* tag's name via the existing `ConflictError`→409 pattern, but allows renaming a tag to its own current name as a no-op) plus `useUpdateTag()`. New `TagDialog.tsx` (name + a color field gated by a "use a custom color" toggle, since a plain color input can't represent "no color"). Delete stays on `ConfirmDialog` (not `GameLibrarySection`'s undo-toast pattern) for both single and bulk, since a tag delete is irreversible and cascades across every game/device/accessory using it — new count-pluralized copy (`deleteConfirmTitle_bulk_one`/`_other` etc.) added alongside the existing name-interpolated single-tag copy, not replacing it.
  - **Follow-up: tag colors everywhere + Tag Manager layout cleanup — done**, per on-screen feedback that a tag's color only showed up in Tag Manager, not on the Game Details Tags chips or the Games list filter chips — plus a second color (chip text color, not just background) and layout complaints (Color column should go, Edit/Delete were spread across the table instead of grouped near Add). Backend: new `Tag.text_color` column (migration `6e0eb5a68977`), threaded through the schema/repository/service/route alongside `color`. New shared `frontend/components/TagChip.tsx` renders a tag's `color`/`textColor` (with an explicit `.MuiChip-deleteIcon` override, since MUI's delete "×" doesn't inherit a custom color sx on its own) — wired into every chip-rendering surface via MUI Autocomplete's `renderValue`/`getItemProps` prop (verified directly against the installed `@mui/material` version's types — not the older `renderTags`/`getTagProps` names memory suggested): `TagsSection.tsx` (Game/Device/Accessory chips) and `GameListToolbar.tsx`'s tag filter (via a new optional `renderValue` passthrough on `AutocompleteMultiSelect.tsx`, byte-identical for every other caller that omits it). `TagDialog.tsx` gained a second native `<input type="color">` for text color, sharing the existing toggle (confirmed with the user: one toggle for both, not two independent ones). `TagManagerPage.tsx` dropped its Color column entirely (kept out of the sortable Name field deliberately, to avoid breaking `EnhancedTable`'s string-based sort comparator) and now uses one combined right-aligned `"actions"` column instead of separate Edit/Delete columns — an additive `EnhancedTable.tsx` change (new `"actions"` headCell id) that doesn't affect its other consumer, `GameLibrarySection.tsx`, which keeps its separate `move`/`edit`/`delete` columns unchanged.
  - See `CHANGELOG.md` for all entries.

- [x] **Allow adding Remake and Port games as top-level entries.** User bug report (GH #6): searching an exact IGDB ID for a Game Boy port of Tennis hit "This category cannot be added." Neither category is an IGDB "addon" (DLC/expansion/pack) — both are independently ownable/playable releases, same bucket as the already-allowed Standalone Expansion/Bundle/Remaster, just missing from the allowlists.
  - Frontend `ADDABLE_CATEGORIES` in `frontend/components/AddGame.tsx` — added `remake` and `port`.
  - Backend `_BROWSABLE_GAME_TYPES` in `backend/app/services/igdb_client.py` already included `remake`(8); added `port`(11) — Port didn't surface from a normal keyword search before this, only via the `igdb:<id>` exact-ID lookup.
  - Addon-fetching (`get_addons_by_parent_igdb_id`) is generic and keyed off the child's own `parent_game` backlink, not the parent's category — a Remake/Port supports having its own addons automatically, no extra logic needed there.
  - **Follow-up fix (GH #8):** a third gate was missed — `_BROWSABLE_CATEGORIES` in `backend/app/repositories/game_repository.py` (controls what shows in the main Games list, separate from the search-result and add-eligibility gates above) still excluded `PORT`, so a Port game imported fine but was invisible everywhere in the app afterward. `REMAKE` had correctly been added there; `PORT` was missed. Fixed by adding it — no data migration needed, since this is a query-time filter, not a stored flag, so already-imported Port games become visible immediately.
  - See `CHANGELOG.md` for the implementation entries.

- [x] **Games page: advanced filtering by tag, console/hardware, and collection/series.** User feedback, then implemented. Full design below. `GameList.tsx`/`GameListToolbar.tsx` previously only offered a debounced name search (min 3 chars) and an owned/wishlist/all chip toggle, both client-side over the full fetched list — the summary payload the list endpoint returns doesn't carry tags/platforms/collections, so richer filtering had to move server-side.
  - **Decided: "console" means the platform of the user's owned copy** (`LibraryItem.platform_id`), not `GamePlatform` (IGDB's full release-platform list) — matches the feedback's own example ("games I have for Game Boy vs PS Vita vs PS5").
  - **Decided: both Collections and Series (backend `Franchise`) are filterable**, as separate axes — matching the two existing browsable sections already in the app (`/games/collections`, `/games/series`).
  - **Decided: filter state stays local component state**, not synced to the URL — matches the page's existing search/owned-wishlist convention. URL-state sync was explicitly declined, not just deferred — not part of this feature going forward.
  - **Phase 1 (backend) — done.** `GET /api/games` gained `platformId`, `tagId` (repeatable, OR semantics), `collectionId`, `franchiseId` query params, threaded through `game_service.search_local_games` into `game_repository.list_top_level_games` (`backend/app/repositories/game_repository.py`) as conditional `exists()` clauses, following the `GET /api/devices` multi-filter precedent. No schema changes — filter dropdowns are populated from the existing unfiltered `GET /api/tags`/`/api/platforms`/`/api/collections`/`/api/franchises` lookups, not from the games list payload. Tests added in `backend/tests/test_game_routes.py`.
  - **Phase 2 (frontend) — done.** `GameListToolbar.tsx` gained a dropdown row (Console/Tag/Collection/Series) beneath the owned/wishlist chips, reusing `AutocompleteSelect` (already proven on `HardwareListToolbar.tsx`) plus a new multi-select sibling `AutocompleteMultiSelect.tsx` for tags. `api/games.ts`'s `listGames`/`useGames` switched from a bare search string to a `GameListFilters` object; `paramsSerializer: { indexes: null }` needed so multi-value array filters serialize as repeated `tagId=1&tagId=2` (matching FastAPI's `list[int]` parsing) instead of axios's default `tagId[]=1`. New i18n keys × all 4 locales.
  - **Refinement pass — done, per on-screen feedback after phase 2 shipped.** (1) The always-visible dropdown row took up too much space — moved into a `Popover` opened from a "Filters" button (badge shows active count) next to the search bar, dropping the toolbar to 2 rows by default; added a "Clear filters" action inside the popover, so the earlier "no separate active-filters row" simplification stands. (2) Console filter now also matches on `Platform.abbreviation` (e.g. typing "PS3" finds "Sony PlayStation 3") via `getOptionLabel` appending the abbreviation — MUI's default filtering picks this up for free. (3) Console became multi-select (e.g. PS3 + PS4 together), mirroring Tag's OR semantics — backend `platformId` query param changed from single int to a repeatable list, same shape as `tagId`, fully backward compatible.
  - See `CHANGELOG.md` for all entries (phase 1, phase 2, and the refinement pass).

- [ ] **Native Android app?** Not yet discussed/scoped — needs a decision on whether this is wanted before any design work starts.
